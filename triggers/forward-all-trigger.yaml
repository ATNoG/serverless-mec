apiVersion: eventing.knative.dev/v1
kind: Trigger
metadata:
  name: forward-all-to-mqtt
  namespace: default
spec:
  broker: default
  filter:
    attributes:
      type: its.cam
  subscriber:
    ref:
      apiVersion: serving.knative.dev/v1
      kind: Service
      name: mqtt-forwarder
