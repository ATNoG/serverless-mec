---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: edgeapplications.
spec:
  group: ""
  names:
    kind: EdgeApplication
    listKind: EdgeApplicationList
    plural: edgeapplications
    singular: edgeapplication
  scope: Namespaced
  versions:
  - name: ""
    schema:
      openAPIV3Schema:
        description: EdgeApplication is the Schema for the edgeapplications API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of EdgeApplication
            properties:
              dId:
                description: dId is the identifier of the edge application descriptor
                  (dId).
                type: string
              dVersion:
                description: dVersion is the version of the application descriptor.
                type: string
              description:
                description: description is a human readable description of the MEC
                  application.
                type: string
              infoName:
                description: infoName is a human readable product name.
                type: string
              instanceId:
                description: instanceId is the application instance identifier.
                type: string
              name:
                description: name is the human readable name of the MEC application.
                type: string
              provider:
                description: provider is the provider of the MEC application.
                type: string
              relatedMeaServices:
                description: relatedMeaServices are edge application services related
                  to this application.
                items:
                  type: string
                type: array
              relatedMepServices:
                description: relatedMepServices are edge platform services related
                  to this application.
                items:
                  type: string
                type: array
              service:
                description: |-
                  service contains vendor-specific Knative configuration used to realize this
                  MEC application as a Knative Service and Trigger (not defined in ETSI GS MEC 010-1).
                properties:
                  container:
                    description: container describes the container configuration for
                      the Knative Service.
                    properties:
                      env:
                        description: env is an optional list of environment variables
                          for the container.
                        items:
                          description: NameValuePair represents a simple name/value
                            pair for environment variables.
                          properties:
                            name:
                              type: string
                            value:
                              type: string
                          required:
                          - name
                          - value
                          type: object
                        type: array
                      image:
                        description: image is the container image for the Knative
                          Service implementing this EdgeApplication.
                        type: string
                    required:
                    - image
                    type: object
                  triggerFilters:
                    additionalProperties:
                      type: string
                    description: |-
                      triggerFilters are the CloudEvent attribute filters for the Knative Trigger
                      (maps to spec.filter.attributes).
                    type: object
                required:
                - container
                type: object
              softVersion:
                description: softVersion is the version of the MEC application software.
                type: string
              usedDNSRules:
                description: usedDNSRules references DNSRule resources used by this
                  app (active rules only).
                items:
                  description: EdgeRuleRef represents a reference to another MEC resource
                    (e.g. TrafficRule, DNSRule).
                  properties:
                    name:
                      description: name of the referenced resource (required in CRD)
                      type: string
                    namespace:
                      description: namespace of the referenced resource (optional
                        in CRD)
                      type: string
                  required:
                  - name
                  type: object
                type: array
              usedTrafficRules:
                description: usedTrafficRules references TrafficRule resources used
                  by this app (active rules only).
                items:
                  description: EdgeRuleRef represents a reference to another MEC resource
                    (e.g. TrafficRule, DNSRule).
                  properties:
                    name:
                      description: name of the referenced resource (required in CRD)
                      type: string
                    namespace:
                      description: namespace of the referenced resource (optional
                        in CRD)
                      type: string
                  required:
                  - name
                  type: object
                type: array
            required:
            - dId
            - dVersion
            - name
            - provider
            - softVersion
            type: object
          status:
            description: status defines the observed state of EdgeApplication
            properties:
              appState:
                description: |-
                  appState corresponds to the ETSI application state:
                  one of "STARTED" or "STOPPED".
                type: string
              conditions:
                description: |-
                  conditions represent the current state of the EdgeApplication resource.
                  Each condition has a unique type and reflects the status of a specific aspect
                  of the resource.

                  Standard condition types may include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              instantiationState:
                description: |-
                  instantiationState corresponds to the ETSI lifecycle state:
                  one of "NOT_INSTANTIATED" or "INSTANTIATED".
                type: string
              operationalState:
                description: |-
                  operationalState is a read-only indicator of operational state:
                  one of "ACTIVE" or "INACTIVE".
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
